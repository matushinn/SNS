# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'SwiftSNS' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SwiftSNS

  pod 'Firebase'
  pod 'Firebase/Auth'
  pod 'Firebase/Storage'
  pod 'Firebase/Firestore'
  pod 'SDWebImage'
  pod "ViewAnimator"
  pod 'ActiveLabel'

  target 'SwiftSNSTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'SwiftSNSUITests' do
    # Pods for testing
  end

end
